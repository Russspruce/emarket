<div class="col-md-4 productSquare">
  <!-- <h3>{{product.name}}</h3> -->
  <img src="{{product.image}}" alt="{{product.name}}" />
  <!-- <ul>
    <li>{{product.description}}</li>
    <li>${{product.price}}</li>
  </ul> -->
</div>
