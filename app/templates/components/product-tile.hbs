<div class="col-md-4 productSquare">
  <!-- <h3>{{product.name}}</h3> -->
  {{#link-to 'product' product.id}}<img src="{{product.image}}" alt="{{product.name}}">{{/link-to}}
  <!-- <ul>
    <li>{{product.description}}</li>
    <li>${{product.price}}</li>
  </ul> -->
</div>
